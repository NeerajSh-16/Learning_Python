# range

# it concists of three arguments
# start value
# end value
# step = it is like common difference of an A.P.

#it will convert 1st n numbers into a list
# we used list() to typecast output of range into a list
n = list(range(10))
print(n)
n = list(range(2,10))
print(n)
n = list(range(2,10,2))
print(n)